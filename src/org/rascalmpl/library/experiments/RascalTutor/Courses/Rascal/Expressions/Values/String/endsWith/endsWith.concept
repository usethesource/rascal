Name: endsWith

Synopsis:

Syntax:

Types:

Function:
`bool endsWith(str s, str suffix)`

Details:

Description:
Yields `true` if string `s` ends with the string `suffix`.

Examples:
<screen>
import String;
endsWith("Hello.rsc", ".rsc");
</screen>

Benefits:

Pitfalls:

Questions:

