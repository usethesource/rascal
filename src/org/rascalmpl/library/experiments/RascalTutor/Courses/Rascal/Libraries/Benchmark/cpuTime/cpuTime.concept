Name: cpuTime

Synopsis: Measuring CPU time.

Syntax:

Types:

Function:
# `int cpuTime()`
# `int cpuTime(void () block)`

Details:

Description:
# Current cpu time in nanoseconds since the start of the thread that runs the code that calls this function.
# The cpu time in nanoseconds used by the execution of the code `block`.

Examples:

Benefits:

Pitfalls:

Questions:

