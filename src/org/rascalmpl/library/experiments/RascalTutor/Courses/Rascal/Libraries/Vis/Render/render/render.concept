Name: render

Details:

Categories: Library

Syntax:

Types:

Function: 
# `void render(Figure fig)`
# `void render(str name, Figure fig)`

Synopsis:

Description: Renders figure `fig` on a canvas. Inside Eclipse a new tabbed pane is created. Used from the command line,
a new window is created.

When `name` is given, it is shown on the tab of the pane and in the window title.

Examples:

Benefits:

Pitfalls:
On Mac OS X, `render` only works inside Eclipse.

Questions:

