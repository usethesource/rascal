Name: size

Synopsis: Determine the number of elements in a list.

Syntax:

Types:

Function:
`int size(list[&T] lst)`

Details:

Description:


Examples:
<screen>
import List;
size([20, 10, 30]);
</screen>

Benefits:

Pitfalls:

Questions:

QValue:
desc: Create a list of the right size.
list:
import List;
list[str] text = <?>;
test: size(text) == 3;