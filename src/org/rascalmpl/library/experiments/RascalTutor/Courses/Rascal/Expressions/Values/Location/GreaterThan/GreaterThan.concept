Name: GreaterThan

Details:

Categories: Language

Syntax:`$Exp_1$ > $Exp_2$`

Types:
| `$Exp_1$` | `$Exp_2$` | `$Exp_1$ > $Exp_2$` |
|:----------|:----------|:---------------------|
| `loc`     |  `loc`    | `bool`               |

Function:

Synopsis:

Description: Yields `true` if the location value of $Exp_1$ strictly textually encloses
the location value of $Exp_2$, and `false` otherwise.

Examples:

Benefits:

Pitfalls:

Questions:
