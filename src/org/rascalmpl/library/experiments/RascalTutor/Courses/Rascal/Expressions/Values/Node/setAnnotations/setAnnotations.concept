Name: setAnnotations

Synopsis: Add anootations to a node value.

Syntax:

Types:

Function:
`&T <: node java setAnnotations(&T <: node x, map[str, value] annotations)`

Details:

Description:
Set the annotations on node value `x` as descriped by the map `annotations`.

Examples:
<screen>
import Node;
setAnnotations("f"(10, "abc"), ("color" : "red", "size" : "large"));
</screen>

Benefits:

Pitfalls:

Questions:

