Name: successors

Synopsis: Determine the set of direct successors of a labeled graph node.

Usage:
`import util::LabeledGraph;`

Types:

Function:
`set[&T] successors(LGraph[&T] G, &T From)`

Details:

Description:
Returns the direct successors of node `From` in LabeledGraph `G`.

Examples:
<screen>
import util::LabeledGraph;
successors({<1,"A",2>, <1,"B",3>, <2,"C",4>, <3,"D",4>}, 1);
</screen>

Benefits:

Pitfalls:

Questions:

