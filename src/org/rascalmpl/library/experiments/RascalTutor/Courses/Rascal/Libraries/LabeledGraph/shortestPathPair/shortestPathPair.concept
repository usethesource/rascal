Name: shortestPathPair

Synopsis: Determine the shortest path between two nodes in a labeled graph.

Syntax:

Types:

Function:
`list[&T] shortestPathPair(LGraph[&T] G, &T From, &T To)`

Details:

Description:
Returns the shortest path between nodes `From` and `To` in LabeledGraph `G`.

Examples:

Benefits:

Pitfalls:

Questions:

