Name: toAngle

Details:

Categories: Library

Syntax:

Types:

Function: 
# `FProperty toAngle(num a)`
# `FProperty toAngle(computedNum ca)`

Synopsis:

Description: Set the end angle of a [wedge] to `a`.
The angle may also be computed by a callback `ca`.

Examples:

Benefits:

Pitfalls:

Questions:

