Name: reducer

Synopsis:

Syntax:

Types:

Function:
`&T reducer(set[&T] s, &T (&T, &T) fn, &T unit)`

Details:

Description:
Apply the function `fn` to successive elements of set `s` starting with `unit`.

Examples:
<screen>
import Set;
int add(int x, int y) { return x + y; }
reducer({10, 20, 30, 40}, add, 0); 
</screen>

Benefits:

Pitfalls:

Questions:

