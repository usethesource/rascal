Name:in

Categories: Language

Related:

Synopsis:
*Syntax: `$Exp$ in $SetExp$`
* Type: `bool`

Description: Yields `true` if the value of $Exp$ occurs as element in the value of $SetExp$ and `false` otherwise. The type of $Exp$ should be compatible with the element type of $SetExp$.

Examples:
<screen>
2 in {1, 2, 3};
4 in {1, 2, 3};
</screen>

Benefits:

Pittfalls:

Questions:

