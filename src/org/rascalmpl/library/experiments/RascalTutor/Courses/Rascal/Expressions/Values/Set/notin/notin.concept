Name:notin

Categories: Language

Related:

Synopsis:
* Syntax: `$Exp$ notin $SetExp$`
* Type: `bool`

Description: Yields `true` if the value of $Exp$ does not occur as element in the value of $SetExp$ and `false` otherwise. The type of $Exp$ should be compatible with the element type of $SetExp$.

Examples:
<screen>
4 notin {1, 2, 3};
4 notin {1, 2, 3, 4};
</screen>

Benefits:

Pittfalls:

Questions:

