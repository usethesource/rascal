Name: isFile

Synopsis: Check whether a file is actually a file (and not a directory).

Syntax:

Types:

Function:
`bool isFile(loc file)`

Details:

Description:
Check whether location `file` is a file.

Examples:

Benefits:

Pitfalls:

Questions:

