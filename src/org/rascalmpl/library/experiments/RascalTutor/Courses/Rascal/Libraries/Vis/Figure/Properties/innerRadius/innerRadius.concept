Name: innerRadius

Details:

Categories: Library

Syntax:

Types:

Function: 
# `FProperty innerRadius(num r)`
# `FProperty innerRadius(computedNum cr)`

Synopsis:

Description: Set the inner radius of a [wedge] to `r`. The value of the inner radius may also be computed by callback `cr`.

Examples:

Benefits:

Pitfalls:

Questions:

