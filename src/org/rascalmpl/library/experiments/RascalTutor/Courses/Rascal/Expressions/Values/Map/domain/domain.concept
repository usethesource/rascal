Name: domain

Synopsis:

Syntax:

Types:

Function:
`set[&K] domain(map[&K, &V] M)`

Details:

Description:
Returns the domain (set of keys) of map `M`.

Examples:
<screen>
import Map;
domain(("apple": 1, "pear": 2));
</screen>

Benefits:

Pitfalls:

Questions:

