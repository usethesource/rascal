Name: parse

Synopsis: Parse a string or file and return a parse tree.

Syntax:

Types:

Function:
# `&T<:Tree parse(type[&T<:Tree] start, str input)`
# `public &T<:Tree parse(type[&T<:Tree] start, loc input)`

Details:

Description:

# Parse a string and return a parse tree.
# Parse the contents of resource input and return a parse tree.

Examples:

Benefits:

Pitfalls:

Questions:

       
