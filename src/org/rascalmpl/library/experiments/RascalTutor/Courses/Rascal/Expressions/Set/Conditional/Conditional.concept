Name:Conditional

Categories: Language

Related:

Synopsis:
* Syntax: `( $BoolExp$ ) ? $SetExp_1$ : $SetExp_2$`
* Type: `set[$Type$]`


Description: Yields the value of $SetExp_1$ if the value of $BoolExp$ is `true` and the value of $SetExp_2$ otherwise. 


Examples:
<screen>
( 3 > 2 ) ? {1, 2} : {3, 4};
( 3 < 2 ) ? {1, 2} : {3, 4};
</screen>

Benefits:

Pittfalls:

Questions:

