Name:mapper

Related:

Synopsis: set[&U] mapper(set[&T] s, &U(&T) fn)

Description: Return a set obtained by applying function <tt>fn</tt> to all elements of set <tt>s</tt>.

Examples:
<screen>
import Set;
int incr(int x) { return x + 1; }
mapper({1, 2, 3, 4}, incr);
</screen>

Benefits:

Pittfalls:

Questions:

