Name: top

Details:

Categories: Library

Syntax:

Types:

Function:
`FProperty top()`

Synopsis:

Description:

`top` defines __top vertical alignment__.
It sets the vertical anchor to 0% of the height of the current figure.
`top()` is equivalent to `vanchor(0.0)`.

Also see [BoxModel] and [vanchor].

Examples:

Benefits:

Pitfalls:

Questions:

