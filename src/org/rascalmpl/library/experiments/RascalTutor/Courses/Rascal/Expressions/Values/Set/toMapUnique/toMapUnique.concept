Name: toMapUnique

Synopsis:

Syntax:

Types:

Function:
`map[&A, set[&B]] toMapUnique(set[tuple[&A, &B]] s) throws DuplicateKey`

Details:

Description:
Convert a set of tuples to a map. The result should be a legal map (i.e., without duplicate keys).

Examples:
<screen>
import Set;
toMapUnique({<"a", 1>, <"b", 2>, <"c", 10>});
// Now explore an erroneous example:
toMapUnique({<"a", 1>, <"b", 2>, <"c", 1>});
</screen>

Benefits:

Pitfalls:

Questions:

