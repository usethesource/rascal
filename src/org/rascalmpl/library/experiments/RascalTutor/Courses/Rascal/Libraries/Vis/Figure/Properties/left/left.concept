Name: left

Synopsis: Left, horizontal, alignment.

Syntax:

Types:

Function:
`FProperty left()`

Details:

Description:
`left` defines __left horizontal alignment__. It sets the horizontal alignment at 1.0.

Note that:
* `left()` is equivalent to `halign(0.0)`.


Also see [align], [halign] and [CompositionModel].

Examples:

Benefits:

Pitfalls:

Questions:

