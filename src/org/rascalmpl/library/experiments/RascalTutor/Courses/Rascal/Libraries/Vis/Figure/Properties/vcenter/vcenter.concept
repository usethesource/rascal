Name: vcenter

Details:

Categories: Library

Syntax:

Types:

Function:
`FProperty vcenter()`

Synopsis:

Description:
`vcenter` defines __vertical centered alignment__.
It sets the vertical alignment to 0.5,

Note that:
* `vcenter()` is equivalent to `valign(0.5)`.


Also see [valign], [align] and [CompositionModel].

Examples:

Benefits:

Pitfalls:

Questions:

