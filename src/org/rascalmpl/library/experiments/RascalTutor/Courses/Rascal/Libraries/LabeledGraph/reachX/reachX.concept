Name: reachX

Details:

Categories: Library

Syntax:

Types:

Function:`set[&T] reachX(LGraph[&T] G, set[&T] Start, set[&T] Excl)`

Synopsis:

Description:
Returns set of nodes in LabeledGraph `G` that are reachable from any of the nodes
in `Start` via path that exclude nodes in `Excl`.
Examples:
<screen>
import LabeledGraph;
reachX({<1,"A",2>, <1,"B",3>, <2,"C",4>, <3,"D",4>}, {1}, {2});
</screen>

Benefits:

Pittfalls:

Questions:

