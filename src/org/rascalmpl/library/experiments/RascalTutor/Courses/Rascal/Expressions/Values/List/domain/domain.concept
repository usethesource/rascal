Name: domain

Synopsis:

Syntax:

Types:

Function:
`set[int] domain(list[&T] lst)`

Details:

Description:
returns the set of all legal index values for a list. Also see [List/index] for a function that returns a list
of index values.

Examples:
<screen>
import List;
domain([1, 3, 5]);
</screen>

Benefits:

Pitfalls:

Questions:

