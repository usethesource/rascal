Name: toInt

Synopsis: Convert a Boolean value to integer.

Syntax:

Types:

Function:
int toInt(bool b)

Details:

Description:
Maps `true` to `1` and `false` to 0.

Examples:
<screen>
import Boolean;
toInt(true);
toInt(false);
</screen>

Benefits:

Pitfalls:

Questions:

