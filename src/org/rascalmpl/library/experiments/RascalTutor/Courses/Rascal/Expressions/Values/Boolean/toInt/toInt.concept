
Name:     toInt
Synopsis: Convert a Boolean value to integer.
Function: int toInt(bool b)

Description:
Maps `true` to `1` and `false` to 0.

Examples:
<screen>
import Boolean;
toInt(true);
toInt(false);
</screen>