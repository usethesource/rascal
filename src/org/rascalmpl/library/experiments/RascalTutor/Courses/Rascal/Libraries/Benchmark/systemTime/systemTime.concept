Name: systemTime

Synopsis: Measuring system time.

Syntax:

Types:

Function:
# `int systemTime()`
# `int systemTime(void () block)`

Details:

Description:
# Current system time in nanoseconds since the start of the thread that runs the code that calls this function.
# System time in nanoseconds needed to execute the code `block`.

Examples:

Benefits:

Pitfalls:

Questions:

