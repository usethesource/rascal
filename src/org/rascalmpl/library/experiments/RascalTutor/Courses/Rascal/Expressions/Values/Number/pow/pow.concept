Name: pow

Synopsis: Calculate the arbitrary power of a numeric value.

Syntax:

Types:

Function: `real pow(num x, int y)`

Details:

Description:
The calculate `x`<sup>`y`</sup> where `y` can only be a integer.

Examples:
<screen>
import Number;
pow(6.4808, 2)
pow(12345678901234567890.0, 1000)
</screen>

Benefits:

Pitfalls:

Questions:

       
