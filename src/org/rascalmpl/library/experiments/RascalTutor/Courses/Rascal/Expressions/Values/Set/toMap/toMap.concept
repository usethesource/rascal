Name: toMap

Synopsis: Convert a set of tuples to a map.

Syntax:

Types:

Function:
`map[&A, set[&B]] toMap(set[tuple[&A, &B]] s)`

Details:

Description:
Convert a set of tuples to a map in which the first element of each tuple is associated with the set of second elements of all tuples with the same first element.

Examples:
<screen>
import Set;
toMap({<"a", 1>, <"b", 2>, <"a", 10>});
</screen>

Benefits:

Pitfalls:

Questions:

