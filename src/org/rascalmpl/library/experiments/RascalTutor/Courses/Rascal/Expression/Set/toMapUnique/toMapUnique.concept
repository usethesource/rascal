Name:toMapUnique

Categories: Library

Related:

Synopsis: map[&A, set[&B]] toMapUnique(set[tuple[&A, &B]] s) throws DuplicateKey

Description: Convert a set of tuples to a map. The result should be a legal map (i.e., without duplicate keys).

Examples:
<warning>toMapUnique seems to be broken</warning>
<screen>
import Set;
toMapUnique({<"a", 1>, <"b", 2>, <"c", 10>});
toMapUnique({<"a", 1>, <"b", 2>, <"a", 10>});
</screen>


Benefits:

Pittfalls:

Questions:

