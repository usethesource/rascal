Name: hcenter

Details:

Categories: Library

Syntax:

Types:

Function:
`FProperty hcenter()`

Synopsis:

Description:

`hcenter` defines __horizontal centered alignment__.
It sets the horizontal anchor at 50% of the width of the current figure.
`hcenter()` is equivalent to `hanchor(0.5)`.

Also see [BoxModel] and [hanchor].

Examples:

Benefits:

Pittfalls:

Questions:

