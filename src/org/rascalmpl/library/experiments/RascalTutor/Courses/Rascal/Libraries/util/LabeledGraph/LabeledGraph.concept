Name: LabeledGraph

Synopsis:
A `LabeledGraph` datatype with associated functions.

Usage:
`import util::LabeledGraph;`

Types:
`alias LGraph[&T] = rel[&T from, &L label &T to];`

Function:

Details:

Description:
The LabeledGraph data type is a binary relation with labeled edges and all operators and functions defined
on [$Prelude/Relation] are also defined on Graph.

Provides the following functions:
<toc Rascal/Libraries/util/LabeledGraph 1>

Examples:

Benefits:

Pitfalls:

Questions:

