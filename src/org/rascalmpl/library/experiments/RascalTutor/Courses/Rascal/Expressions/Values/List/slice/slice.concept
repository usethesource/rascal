Name: slice

Synopsis:

Syntax:

Types:

Function:
`list[&T] slice(list[&T] lst, int start, int len) throws IndexOutOfBounds(int index)`

Details:

Description:
Returns a sublist of `lst` from index `start` of length `len`.

Examples:
<screen>
import List;
slice([10, 20, 30, 40, 50, 60], 2, 3);
</screen>

Benefits:

Pitfalls:

Questions:

