Name: toInt

Synopsis:

Syntax:

Types:

Function:
`int toInt(str s) throws IllegalArgument(value v)`

Details:

Description:
Converts string `s` to integer. Throws `IllegalArgument` when `s` cannot be converted.

Examples:
<screen>
import String;
toInt("2010");
toInt("abc");
</screen>

Benefits:

Pitfalls:

Questions:

