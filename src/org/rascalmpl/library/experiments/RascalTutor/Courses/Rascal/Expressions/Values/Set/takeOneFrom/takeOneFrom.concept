Name: takeOneFrom

Synopsis:  Remove an arbitrary element from a set.

Syntax:

Types:

Function:
`tuple[&T, set[&T]] takeOneFrom(set[&T] s)`

Details:

Description:
Remove an arbitrary element from set `s` and return a tuple consisting of the element and the modified set.

Examples:
<screen>
import Set;
takeOneFrom({1, 2, 3, 4});
takeOneFrom({1, 2, 3, 4});
takeOneFrom({1, 2, 3, 4});
</screen>

Benefits:

Pitfalls:

Questions:

