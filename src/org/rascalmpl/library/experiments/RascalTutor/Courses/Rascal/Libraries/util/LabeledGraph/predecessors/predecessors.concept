Name: predecessors

Synopsis: Determine the set of direct predecessors of a node in a labeled graph.

Usage:
`import util::LabeledGraph;`

Types:

Function:
`set[&T] predecessors(LGraph[&T] G, &T From)`

Details:

Description:
Returns the direct predecessors of node `From` in LabeledGraph `G`.

Examples:
<screen>
import util::LabeledGraph;
predecessors({<1,"A",2>, <1,"B",3>, <2,"C",4>, <3,"D",4>}, 4);
</screen>

Benefits:

Pitfalls:

Questions:

