Name: orientation

Details:

Syntax:<br>
`FProperty orientation(Orientation o)`

<listing>
data Orientation =
		leftRight()
	|	rightLeft()
	| 	topDown()
	|	downTop();
</listing>

Types:

Function:

Synopsis:

Description: Describes the orientation of a [graph] or a [tree]. Possible values are described by
the datatype `orientation`.



Examples:

Benefits:

Pitfalls:

Questions:

       
