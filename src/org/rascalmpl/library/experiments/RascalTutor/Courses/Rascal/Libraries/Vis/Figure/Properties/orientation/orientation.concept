Name: orientation

Synopsis: Define the orientation of a [tree] or [$Figures/graph].

Syntax:

Types:

<listing>
data Orientation =
		leftRight()
	|	rightLeft()
	| 	topDown()
	|	downTop();
</listing>



Function:
`FProperty orientation(Orientation o)`

Details:

Description:
Describes the orientation of a [$Figures/graph] or a [tree]. Possible values are described by
the datatype `orientation`.

Examples:

Benefits:

Pitfalls:

Questions:

