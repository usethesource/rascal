Name: LabeledGraph

Synopsis:
A `LabeledGraph` datatype with associated functions.

Syntax:
`import LabeledGraph;`

Types:
`alias LGraph[&T] = rel[&T from, &L label &T to];`

Function:

Details:

Description:
The LabeledGraph data type is a binary relation with labeled edges and all operators and functions defined
on [Relation] are also defined on Graph.

Examples:

Benefits:

Pitfalls:

Questions:

