Name: rangeX

Details:

Categories: Library

Syntax:

Types:

Function: `map[&K, &V] rangeX(map[&K, &V] M, set[&V] S)`

Synopsis:

Description: Returns the map restricted to pairs with values not in `S`.

Examples:
<screen>
import Map;
rangeX(("apple": 1, "pear": 2, "ananas": 3), {2, 3});
</screen>

Benefits:

Pittfalls:

Questions:

