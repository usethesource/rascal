Name: startsWith

Synopsis: Check whether a string value starts with a given substring.

Syntax:

Types:

Function:
`bool startsWith(str s, str prefix)`

Details:

Description:
Yields `true` if string `s` starts with the string `prefix`.

Examples:
<screen>
import String;
startsWith("Hello.rsc", "Hell");
</screen>

Benefits:

Pitfalls:

Questions:

