Name: size

Synopsis:

Syntax:

Types:

Function:
`int size(set[&T] s)`

Details:

Description:
Returns the number of elements in `s`.

Examples:
<screen>
import Set;
size({1,2,3,4});
size({});
</screen>

Benefits:

Pitfalls:

Questions:
QValue:
prep: import Set;
make: N = int[0,5]
hint: <N> values separated by commas
test: size({ <?> }) == <N>

