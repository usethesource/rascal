Name: hint

Synopsis: Hint at layout algorithm to be used in graphs.

Syntax:

Types:

Function:
`hint(str name)`

Details:

Description:
Gives a hint for the layout strategy to be used. Currently implemented hints all apply to the [graph] layout:
* `spring`: use force-directed spring layout (currently disabled).
* `layered`: draw as a layered graph (the default).

Examples:

Benefits:

Pitfalls:

Questions:

