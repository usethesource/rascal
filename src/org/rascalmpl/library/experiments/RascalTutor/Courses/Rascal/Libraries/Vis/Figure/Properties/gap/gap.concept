Name: gap

Details:

Categories: Library

Syntax:

Types:

Function:
`FProperty gap(num h, num v)`


Synopsis:

Description:
Set the horizontal gap between figures to `h` and the vertical gap to `v`. 

Also see [hgap] and [vgap].

Note that:
* `gap(hv)` is equivalent to `gap(hv, hv)` and to `hgap(hv), vgap(hv)`.
* `gap(h, v)` is equivalent to `hgap(h), vgap(v)`.

Examples:

Benefits:

Pitfalls:

Questions:

