Name: reach

Synopsis: Determine the labeled graph nodes reachable from a set of nodes.

Syntax:

Types:

Function:
`set[&T] reach(LGraph[&T] G, set[&T] Start)`

Details:

Description:
Returns the set of nodes in LabeledGraph `G` that are reachable from any of the nodes
in the set `Start`.

Examples:

Benefits:

Pitfalls:

Questions:

