Name: mapper

Synopsis:

Syntax:

Types:

Function:
`set[&U] mapper(set[&T] s, &U(&T) fn)`

Details:

Description:
Return a set obtained by applying function `fn` to all elements of set `s`.

Examples:
<screen>
import Set;
int incr(int x) { return x + 1; }
mapper({1, 2, 3, 4}, incr);
</screen>

Benefits:

Pitfalls:

Questions:

