module lang::rascalcore::compile::Examples::Tst1


import ParseTree;


// we only allow declarations on Tree for now, for lack of a syntax to declare them on non-terminals.
//data Tree(str y = "y");

//&T<:Tree get(&T<:Tree e) = e;

value main() //test bool charTest() 
    = char(32); //.y; //== "y";
