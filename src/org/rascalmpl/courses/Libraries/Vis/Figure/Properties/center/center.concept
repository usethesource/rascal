Name: center

Synopsis: Centered alignment.

Syntax:

Types:

Function:
`FProperty center()`

Details:

Description:
`center` defines __horizontal and vertical centered alignment__.
It sets the horizontal and vertical alignment to 0.5.

Note that:
* `center()` is equivalent to `anchor(0.5, 0.5)`.


Also see [align] and [CompositionModel].

Examples:

Benefits:

Pitfalls:

Questions:

