Name: LessThan

Synopsis: The less than operator on location values.

Syntax:
`$Exp_1$ < $Exp_2$`

Types:
| `$Exp_1$` | `$Exp_2$` | `$Exp_1$ < $Exp_2$` |
|:----------|:----------|:---------------------|
| `loc`     |  `loc`    | `bool`               |

Function:

Details:

Description:
Yields `true` if the location value of $Exp_1$ is strictly textually contained
in the location value of $Exp_2$, and `false` otherwise.

Examples:

Benefits:

Pitfalls:

Questions:

