Name: GreaterThanOrEqual

Synopsis: The greater than or equal operator on location values.

Syntax:
`$Exp_1$ >= $Exp_2$`

Types:
| `$Exp_1$` | `$Exp_2$` | `$Exp_1$ >= $Exp_2$` |
|:----------|:----------|:---------------------|
| `loc`     |  `loc`    | `bool`               |

Function:

Details:

Description:
Yields `true` if the location value of $Exp_1$ textually encloses
the location value of $Exp_2$, and `false` otherwise.

Examples:

Benefits:

Pitfalls:

Questions:

