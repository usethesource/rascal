Name: Descriptive
Synopsis: Descriptive Statistics.

Description:
Provides the following univariate (single variable) statistics functions:

<toc Rascal/Libraries/stat/Descriptive 1>

Examples:

<screen>
import analysis::statistics::Descriptive;
D = [67, 88, 55, 92.5, 102, 51];
mn = min(D);
mx = max(D);
range = mx - mn;
midrange =  mn + range/2;
sum(D);
mean(D);
geometricMean(D);
standardDeviation(D);
variance(D);
percentile(D,25);
percentile(D,50);
percentile(D,75);
</screen>
