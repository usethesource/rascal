# Invalid Use Of DateTime

.Synopsis
Invalid runtime use of a datetime value.

Types
`data RuntimeException = InvalidUseOfDateTime(str message);`
       
.Usage
`import Exception;` (only needed when `InvalidUseOfDateTime` is used in `catch`)
