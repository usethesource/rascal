# Declarations Command

.Synopsis
Show all declarations and imports in current RascalShell session.

.Syntax
`declarations`

.Description
All imports and declarations entered during the current session are shown.
Also see <<Undeclare Command>> to remove a declaration or 
<<Unimport Command>> to remove an import from the current session.

.Examples

[source,rascal-shell]
----
real pi = 3.14;
data D = d1(int n) | d2(str s);
declarations
----