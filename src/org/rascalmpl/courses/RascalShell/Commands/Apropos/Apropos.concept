# Apropos Command

.Synopsis
Get help and search documentation, results shown in console.

.Syntax
* `apropos`
* `apropos _Topics_`

.Description

Rascal provides a help system as described in link:/GettingHelp[Getting Help].

The `apropos` command is similar to <<Help Command>>, 
but results are only shown in the RascalShell console
(no browser window is opened).

