# Test Command

.Synopsis
Run tests.

.Syntax
* `test`
* `test _Modules_`

.Description

Run Rascal tests.

In the first form, the tests in all currently imported modules are executed.

In the second form, the tests in the given list of _Modules_ are executed.

.Examples
Execute the tests in an imported module:

[source,rascal-shell]
----
import demo::basic::Factorial;
test
----

Execute the tests in the `Integers` module in the Rascal test suite:
[source,rascal-shell]
----
test lang::rascal::tests::basic::Integers
----